- Python 3.13

- use `uv run python` to execute Python scripts
- use `uv add` to install packages/libraries

- use context7 to get most up-to-date documentation on packages and APIs (e.g., OpenAI API)
- prefer concise, DRY and imperative code
- do not wrap everything in print statements
